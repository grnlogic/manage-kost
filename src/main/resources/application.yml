server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

spring:
  mail:
    enabled: false # Email disabled - OTP shown in console only
    host: smtp.gmail.com
    port: 587
    username: geranuser@gmail.com
    password: uhrm ribj pfah bdps
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    debug: false

  datasource:
    url: jdbc:postgresql://localhost:5432/kos_app_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
      idle-timeout: 30000
      max-lifetime: 1800000
    fail-fast: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
      javax:
        persistence:
          schema-generation:
            database-action: none

  flyway:
    enabled: false

  main:
    allow-bean-definition-overriding: true
    allow-circular-references: true

# Add custom logging configuration
logging:
  level:
    root: INFO
    MenejementKos: DEBUG
    org.springframework.web: DEBUG
    org.hibernate: DEBUG
    com.zaxxer.hikari: DEBUG

# JWT Configuration
jwt:
  secret: secureJwtSecretKeyForKosAppMustBeAtLeast32Chars
  expiration: 86400000 # 24 hours in milliseconds
