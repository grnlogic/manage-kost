# ========================================
# Environment Variables untuk Kos App
# ========================================

# ========================================
# DEVELOPMENT (Localhost PostgreSQL)
# ========================================
# Gunakan profile dev untuk development
# Command: mvn spring-boot:run -Dspring-boot.run.profiles=dev
# 
# Config ada di: src/main/resources/application-dev.yml
# Database: localhost:5432/kos_app_db
# Username: postgres
# Password: postgres
# 
# TABEL OTOMATIS TERBUAT! ✨
# Tidak perlu create tabel manual karena ddl-auto: update

# ========================================
# PRODUCTION (Railway PostgreSQL)
# ========================================
# Set environment variables di Railway:

# Database Connection (dari Railway Internal Connection)
DATABASE_URL=postgresql://postgres:password@host:5432/railway
PROD_DB_USERNAME=postgres
PROD_DB_PASSWORD=your_database_password

# JWT Configuration
JWT_SECRET=secureJwtSecretKeyForKosAppMustBeAtLeast32Chars
JWT_EXPIRATION=86400000

# Server Port (Railway akan set otomatis)
PORT=8080

# Logging Level
LOG_LEVEL=INFO
SHOW_SQL=false

# ========================================
# Email Configuration (Gmail SMTP)
# ========================================
# Sudah di-hardcode di application.yml
# Jika ingin ubah, bisa override dengan env var:

# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=geranuser@gmail.com
# MAIL_PASSWORD=uhrm ribj pfah bdps

# ========================================
# CARA MENGGUNAKAN
# ========================================

# 1. DEVELOPMENT:
#    - Database localhost akan auto-create tabel
#    - Jalankan: mvn spring-boot:run -Dspring-boot.run.profiles=dev

# 2. PRODUCTION (Railway):
#    - Set semua env var di atas di Railway dashboard
#    - Database Railway akan auto-create tabel saat deploy
#    - Railway auto-detect dan run aplikasi

# ========================================
# NOTES
# ========================================
# - Database harus sudah ada (kos_app_db untuk dev)
# - Tabel-tabel akan otomatis terbuat oleh Hibernate
# - Jangan commit file .env yang berisi credentials!
